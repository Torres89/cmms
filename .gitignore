.env
.env.prod
.env.local
**/.env.local
.env.genaiscript
.genaiscript
.DS_Store
**/.DS_Store
atlas_backups
docker-compose-dev.yml
logo
deep-research-report.html
deep-research-report.md
docs/plans/
# cmms-agent env
cmms-agent/.env

# AWS keypairs (sensitive)
keypair/

# Local Claude Code skills (project-specific, not shared)
.claude/

# Internal documentation
lessons_learned/

# Reverse proxy config (contains hostnames/secrets; never publish)
Caddyfile

# Python virtualenvs and bytecode. These were tracked by mistake: the cmms-agent
# venv is 204 MB of Windows-only .pyd/.exe files that the Linux images rebuild
# from requirements.txt anyway, and every .pyc showed up as a spurious diff.
venv/
**/venv/
.venv/
**/.venv/
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Reverse proxy config - lives only on the box, never committed
Caddyfile

# Timestamped .env backups taken during deploys - full of live secrets
.env.backup*
